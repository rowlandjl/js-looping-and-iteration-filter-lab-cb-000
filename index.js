function findMatching(drivers, str) {
  return drivers.filter(function(driver) {return driver.toLowerCase() == str;});
}


function fuzzyMatch(drivers, str) {
  
}


function matchName(drivers, str) {
  
}